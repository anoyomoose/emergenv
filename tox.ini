[tox]
envlist = py39, py310, py311, py312, py313, py314
isolated_build = True

[testenv]
description = Run the test suite under {basepython}
extras = test
commands = pytest {posargs}

# `age` lives on PATH; tox already passes PATH through. HOME is needed because a
# couple of tests look under ~/.ssh.
passenv = HOME
